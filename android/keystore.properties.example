# Copy to android/keystore.properties only in the secured release environment.
# Paths are resolved relative to android/ unless an absolute path is used.
storeFile=<absolute-or-android-relative-upload-keystore-path>
storePassword=<set-locally>
keyAlias=<upload-key-alias>
keyPassword=<set-locally>
